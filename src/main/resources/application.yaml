camel:
  springboot:
    main-run-controller: true
spring:
  artemis:
    user: admin
    password: admin

app:
  queue:
    consume: "jms:queue:CONSUMER_QUEUE"
    produce: "jms:queue:RECEIVER_QUEUE"